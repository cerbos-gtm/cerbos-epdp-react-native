# Copy to `.env`. The rule ID is in Cerbos Hub: your deployment > Embedded PDP
# rules. The rule must allow public access.
EXPO_PUBLIC_CERBOS_HUB_RULE_ID=

# Set to 1 to run the latency benchmark (ePDP tab) at startup and log the result.
EXPO_PUBLIC_CERBOS_BENCH=
