# Copy to `.env` and fill in the ID of your ePDP policy bundling rule.
# Find it in Cerbos Hub: your deployment > "Embedded PDP rules" tab.
# The rule must allow public access (no client credentials), which is the
# appropriate setting for bundles downloaded directly by mobile apps.
EXPO_PUBLIC_CERBOS_HUB_RULE_ID=
